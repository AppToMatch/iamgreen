import 'package:flutter/material.dart';

class MapSceen extends StatelessWidget {
  const MapSceen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
