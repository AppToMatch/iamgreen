import 'package:flutter/material.dart';

class DonateSceen extends StatelessWidget {
  const DonateSceen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
